fun bubbleSortWithSteps (numbers) : Int{
    //println ("Initial numbers [%s]".format(numbers.joinToString(separator =", ")))
    for ( pass > pass +(numbers+1) ) {
        //comentario
       
    }
}